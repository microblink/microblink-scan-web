import { RotationCorrectionPipe } from './rotation-correction.pipe';

describe('RotationCorrectionPipe', () => {
  it('create an instance', () => {
    const pipe = new RotationCorrectionPipe();
    expect(pipe).toBeTruthy();
  });
});
